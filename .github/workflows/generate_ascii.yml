name: generate ascii artwork
on: push
jobs:
    first_job:
     runs-on: ubuntu-latest
     steps: 
     - name: checkout repo
       uses: actions/checkout@v6

     - name: Executing a script
       run: ./ascii_txt.sh

     - name: list
       run: ls -lrta
